name = "envpact-dashboard"
compatibility_date = "2026-01-01"
pages_build_output_dir = "./dist"

# Bound to both the static build and the Pages Functions runtime.
# Public OAuth client_id — safe to ship in plaintext (it has no
# corresponding client_secret in the device flow).
[vars]
PUBLIC_GITHUB_OAUTH_CLIENT_ID = "Ov23li6fh0vJLw1VNcJf"
